const { getCollectionFloorTon } = require('../marketProxy');

// Future-ready: If we add more sources later, we can aggregate here.
// For now, we rely entirely on the Proxy.

module.exports = { getCollectionFloorTon };